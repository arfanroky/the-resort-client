import { useState } from "react"

const useGetRoom = (room) => {
    // const [roomList, setRoomList] = useState(room);

    console.log(room);

    // return [roomList, setRoomList]
}

export default useGetRoom;